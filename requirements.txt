# --- Framework API & Serveur ---
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
python-multipart

# --- Data Science & IA ---
numpy>=1.21.0
pandas>=1.5.0
scikit-learn>=1.7.2
joblib>=1.2.0

# --- Base de Données & Sécurité ---
SQLAlchemy>=2.0.0
# CORRECTIFS SÉCURITÉ :
# 1. On ajoute le moteur de calcul pour Argon2 (utilisé dans auth.py)
argon2-cffi>=23.1.0
# 2. On force la version 3.1.7 de bcrypt pour éviter le bug "__about__" avec passlib
bcrypt==3.1.7
# 3. On installe passlib avec les deux supports
passlib[argon2,bcrypt]>=1.7.4
# 4. Version stable pour JWT
python-jose[cryptography]>=3.3.0

# --- Gestion des requêtes ---
requests>=2.28.0